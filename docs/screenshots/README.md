# Screenshots

Purpose: store UI captures referenced from the root README.

Suggested files (add after a local run):

| File | View |
| --- | --- |
| `dashboard.png` | Readiness overview |
| `employee-dialog.png` | Create / edit employee |
| `notifications.png` | Open findings / notifications |
| `audit.png` | Admin audit trail |
| `login.png` | Sign-in page |

Keep images reasonably sized (roughly ≤ 1600 px wide) so the README stays fast to load. Prefer PNG for UI chrome.
